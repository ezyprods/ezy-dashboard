'use client';

/**
 * AudioMetadataWorker (Disabled)
 * 
 * Audio feature analysis is performed on-demand during upload (SmartUploadModal)
 * or via manual trigger to prevent massive background data transfer.
 */
export function AudioMetadataWorker() {
  return null;
}

